<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title%></title>
	</head>
	<body>
		<h2><%= title%></h2>
		<%if(user) {%> <%if(posts.length > 0){%> <% for (let post of posts) {%>
		<h2><%= post.title%></h2>
		<h3><%= post.post%></h3>
		<%if(user.membership_status === 'Member' || user.admin) {%>
		<h5><%= post.timestamp %></h5>
		<h5>Post by: <%= post.user.fullname%></h5>
		<%}%> <%if(user.admin) {%>
		<button><a href="/posts/<%=post.url%>/delete">Delete Post</a></button>
		<%}%> <%}%> <%}else{%>
		<h3>
			No posts yet. To create a post click
			<a href="<%= user.url%>/create-post">HERE</a>
		</h3>
		<%}%>
		<hr />
		<button><a href="<%= user.url%>">VIEW PROFILE</a></button>
		<br />
		<%} else {%>
		<Button><a href="/log-in">LOGIN</a></Button>
		<br />
		<button><a href="/sign-up">SIGN-UP</a></button>
		<%}%>
	</body>
</html>
