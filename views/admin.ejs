<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title%></title>
	</head>
	<body>
		<h2><%= title%></h2>
		<%if(info) {%>
		<p><%= info%></p>
		<%}%>
		<form action="" method="post">
			<label for="admin">Enter your code here: </label><br />
			<input type="text" name="admin" required /><br />
			<button type="submit">Submit</button>
		</form>
		<%if(errors) {%>
		<ul>
			<%for(let error of errors){%>
			<li><%= error.msg%></li>
			<%}%>
		</ul>
		<%}%>
	</body>
</html>
